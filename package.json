{
  "name": "@krabba/ui",
  "author": "Krabba",
  "version": "0.0.1",
  "license": "MIT",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/**"
  ],
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "dev": "pnpm --filter ./apps/react-vite dev",
    "build:packages": "tsc -b packages/*",
    "build": "pnpm build:packages"
  },
  "dependencies": {
    "@changesets/cli": "^2.27.10"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
